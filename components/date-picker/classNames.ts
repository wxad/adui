const prefix = "adui-date"

export default {
  body: `${prefix}-body`,
  caption: `${prefix}-caption`,
  day: `${prefix}-day`,
  disabled: `${prefix}-disabled`,
  month: `${prefix}-month`,
  months: `${prefix}-months`,
  navBar: `${prefix}-navBar`,
  navButtonNext: `${prefix}-navButtonNext`,
  navButtonPrev: `${prefix}-navButtonPrev`,
  outside: `${prefix}-outside`,
  selected: `${prefix}-selected`,
  today: `${prefix}-today`,
  week: `${prefix}-week`,
  weekday: `${prefix}-weekday`,
  weekdays: `${prefix}-weekdays`,
  weekdaysRow: `${prefix}-weekdaysRow`,
  weekNumber: `${prefix}-weekNumber`,
  wrapper: `${prefix}-wrapper`,
}
